# Configuration samples

The P1 qualification workflow reads scoring and routing rules from Airtable.
Use these fictional CSV rows to seed disposable test tables, then review every
weight, threshold, owner, and follow-up interval before testing.
